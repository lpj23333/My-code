#include <stdio.h>

int main()
{
	int i;
	int a[5];
	int *p = a;
	
	printf("请输入正整数"); 
	for(i = 0;i < 5;i++){
		scanf("%d" , p++);
	} 
	p = a;
	for(i = 0;i < 5;i++, p++)
		printf("%d", *p);
	printf("\n");
	
	return 0;
}
